# 弹幕字形、LIVE 排序和底部跟随

- 字体改成先描边、再单独覆盖完整字形，避免居中的描边侵入笔画。真机逐像素对比确认不透明字形内部保持白色；附件包含实际位图。
- 展开的头像轮盘收到直播状态时立即将直播 UP 前置，不再受展开时固定顺序限制；当前焦点按 ID 保留。关注页首次加载也主动更新直播目录。
- 弹幕使用 ScrollPosition 的底边定位与内容尺寸变化底部锚点，并按最后消息 ID 触发跟随。删除会将新消息导致的高度增长误判成用户离底的判断。

18 项真机回归通过，0 失败、0 跳过。仅使用 iPhone 17，无 Simulator。
验证包含：字形内部像素、展开后异步到达的直播状态/实际头像截图、连续四批共 360 条消息与历史裁剪后的 UIScrollView 底部位置、上下文菜单和直播目录回归。
首次列表回归揭示按末条 ID 定位在裁剪后偏移 74–81pt；改用底边后通过，未放宽断言。
完整结果：/tmp/NeoBiliDanmakuFollowupFinal-20260912.xcresult。
LIVE 截图采用本地确定性 UP 夹具，验证异步更新后的实际 UIKit/SwiftUI 显示；本轮未再次对用户真实关注列表截图。

Release 已在 iPhone 17 安装并启动，见 install.json、launch.json。
