# 返回手势、标题、评论玻璃控件与小窗边界

2026-09-10，iPhone 17 真机；无 Simulator。

- 内容区在播放器尚未完全展开时拒绝系统返回的竖向 pan，横向返回与原代理规则保留。播放器区域与内容区共享同一个 delegate gate，反复布局不互相覆盖，最后一个区域拆除时恢复系统原代理。
- 标题不再截行，随行数增加容器高度；独立48pt箭头只切换简介正文。评论点赞与回复使用原生玻璃按钮，回复文字固定单行，保留既有操作和点赞状态。
- 设置增加小窗上下边界（当前宿主安全可用高度的百分比），默认0–100%，至少45%间距。拖动/停靠/画幅改变均受约束，空间不足时等比缩小。滑杆调整时用0.22透明度粉色区域与0.85透明度1.5pt边界线实时显示，松手1秒后淡出，不命中触摸。

## 验证

`/tmp/NeoBiliInteractionRefinement-20260910.xcresult`：46项通过。覆盖原播放、小窗交接与拖动、滚动展开，以及新加的返回方向/共享代理/恢复系统代理、上下边界非法值处理和横竖视频约束。手势测试是对生产 delegate/UIScrollView 桥接输入受控事件，不是自动化真实手指滑动证明。

截图复查发现回复换行，修复后`/tmp/NeoBiliCommentGlassVerified-20260910.xcresult`单项三态界面测试通过并人工查看，标题完整、回复单行、小窗与范围线对齐。首次单跑截图遇到测试宿主未进入前台，测试改为等待前台就绪；未删除断言或改为跳过。截图使用真机窗口中的生产组件与本地静态数据，不调用点赞/回复写接口。

Debug测试构建与最终Release构建均通过。README已补充范围设置和交互说明。

Release 安装成功，序号5308；启动成功，PID27194。devicectl结果为`/tmp/neobili-interaction-refinement-install.json`与`/tmp/neobili-interaction-refinement-launch.json`。
