# 播放器单行控件与紧凑外观

顶部顺序：返回、可用时的单行标题、分辨率、音质、弹幕、省略号。底部进度条与全屏按钮共用一行并占满可用宽度，间隔 6pt；不再输出第二排收缩操作，收缩保留在菜单中。
控件可见玻璃高 32pt，布局/点击区域保留 48pt；中央播放按钮最大 72pt。高度不足以容纳中央按钮时，播放操作收进更多菜单。极矮的收缩条仅保留底栏，避免重叠点击区域。

原生 Menu 独立负责 48pt 点击区域，可见标签单独附着 32pt 玻璃，避免 Menu 自动放大玻璃及玻璃采样模糊文字。标签不拦截点击，菜单继续提供名称和当前值给辅助功能。

验证：iPhone 17 真机 11 项测试全部通过；包含多个画幅、横竖屏、加载状态、长画质文本、控件不重叠和底栏宽度检查。实际截图已检查，见 inline-controls.png。
未使用 Simulator。最终结果包：/tmp/NeoBiliChromeRowsFinalLabel-20260912.xcresult。
中间一次加入菜单点击手势的构建在旋转截图测试中崩溃；该手势调整已撤回，最终版保留原按钮状态观察方式并通过回归。

Release 构建通过，已安装并启动在 iPhone 17；见 install.json、launch.json。

后续贴边调整：横屏两侧布局留白 28→16pt，其余模式 16→8pt；顶部额外留白 6→0pt（横屏最低 4pt），底部额外留白 8→0pt。32pt 玻璃和 48pt 点击区保持；安全区保留。两项真机多画幅/黑边截图测试通过，见 edge-test-summary.json、edge-landscape.png。结果包 /tmp/NeoBiliPlayerEdges-20260912.xcresult。
